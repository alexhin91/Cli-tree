This python script can be used as a vendor agnostic way to crawl the entire CLI of a given 
device, being a switch,router,firewall, etc. From that point it will visualize the entire
tree of the CLI as to give the user an overall idea how how to entire CLI is structured.